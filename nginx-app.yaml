apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nginx-deployment
  namespace: argocd
spec: 
 destination:
   namespace: default
   server: https://kubernetes.default.svc
 project: default
 source:
   helm:
    valueFiles:
    - values.yaml
   path: nginx-deployment 
   repoURL: https://github.com/joosequezada/helm-k8s-prj
   targetRevision: master
